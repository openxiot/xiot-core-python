[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "xiot-core"
version = "0.0.8"
dependencies = []

[tool.setuptools]
package-dir = {"" = "src"}
packages = {find = {where = ["src"]}}